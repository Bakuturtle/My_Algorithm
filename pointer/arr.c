#include <stdio.h>
#include <stdlib.h>

int main(void){

    // int** arr = malloc(sizeof(int) * 5);
    // for(int i=0; i < 5; i++)
    //     arr[i] = malloc(sizeof(int)*5);
    
    int arr[25] = {0,};

    return 0;
}